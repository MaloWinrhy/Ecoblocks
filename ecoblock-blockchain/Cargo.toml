[package]
name = "ecoblock-blockchain"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = "4.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10.8"
rand = "0.8"
chrono = "0.4"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
